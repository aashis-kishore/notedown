import styled from "styled-components";

const StyledMiddlePane = styled.div`
  display: flex;
  height: 90vh;

  @media screen and (max-width: 480px) {
    position: relative;
  }
`;

export default StyledMiddlePane;
